<!-- Plugin Configuration File. Read more: https://plugins.jetbrains.com/docs/intellij/plugin-configuration-file.html -->
<idea-plugin>
    <id>dev.gaphunter.elasticsearchclientreusecompanion</id>
    <name>Elasticsearch Client Reuse Companion</name>
    <vendor>Gap Hunter Labs</vendor>
    <description><![CDATA[
        <p>Warning icon on a <code>new RestHighLevelClient(...)</code>
        (legacy client) or <code>new ElasticsearchClient(...)</code>
        (modern Java client) construction written inside a regular
        method body -- Elastic's own documentation states "only one
        single instance should be created" and that it's "best
        practice to create your RestHighLevelClient instance as
        singleton" since "each instance creates its own connection
        pool". Building one inside a regular method means a brand new
        connection pool on every call.</p>
        <p><b>100% static text/PSI analysis</b> -- matches the class
        name by simple text, so it works whether the real Elasticsearch
        client jar is on the classpath or not. Java and Kotlin.</p>
        <p>Stated honestly: v0.1 only flags the direct <code>new</code>
        construction shape -- a client obtained by reference from an
        existing shared instance/dependency injection is never flagged
        (correctly, since it isn't the anti-pattern this plugin
        targets).</p>
        <p>Free. No license nags, no telemetry, no network calls.</p>
    ]]></description>

    <depends>com.intellij.modules.platform</depends>
    <depends>com.intellij.java</depends>
    <depends>org.jetbrains.kotlin</depends>

    <extensions defaultExtensionNs="org.jetbrains.kotlin">
        <supportsKotlinPluginMode supportsK1="true" supportsK2="true"/>
    </extensions>

    <extensions defaultExtensionNs="com.intellij">
        <codeInsight.lineMarkerProvider language="JAVA"
            implementationClass="dev.gaphunter.elasticsearchclientreusecompanion.gutter.ClientBuiltPerCallLineMarkerProvider"/>
        <codeInsight.lineMarkerProvider language="kotlin"
            implementationClass="dev.gaphunter.elasticsearchclientreusecompanion.gutter.ClientBuiltPerCallLineMarkerProvider"/>
        <notificationGroup id="Elasticsearch Client Reuse Companion" displayType="BALLOON"/>
    </extensions>
</idea-plugin>
